import { createTheme } from '@mui/material/styles';

export const customedPalette = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00bcd4',
    },
    secondary: {
      main: '#fb8c00',
    },
  },
  });